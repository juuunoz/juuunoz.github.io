export const BodyEntry = ({children} : {children: React.ReactNode}) => {
    return (
      <div 
        className=""
        id="bodyContent"
        >
            {children}
        </div>
    );
  }